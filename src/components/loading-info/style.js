import styled from 'styled-components'
import { colors } from '../../shared/style/colors'

const LoadingInfoStyled = styled.div`
  background: ${colors.nonPhotoBlue};
  color: ${colors.night};
  font-size: 2em;
  height: 60vh;
  padding-top: 40vh;
  text-align: center;
  width: 100vw;
`

export default LoadingInfoStyled